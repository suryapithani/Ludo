<html>
<head>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.0/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.0.4/socket.io.js"></script>

    <!-- CSS only -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <link rel="stylesheet" type="text/css" href="<%= baseUrl %>registration.css"/>

    <title>Ludo - Register</title>
</head>

<body>
        <div class="card">
            <div class="card-body">
                <h2>LUDO - Register here! </h2><br>
                <form class="needs-validation" name="register-form">
                    <div class="mb-3">
                        <label for="username" class="form-label" >Enter your username</label>
                        <input type="text" class="form-control" id="username" placeholder="Username">
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label" >Enter your password<br >(6 to 20 characters which contain at least one numeric digit, one uppercase and one lowercase letter)</label>
                        <input type="password" class="form-control" id="password" placeholder="Password" name="password">
                    </div>
                    <div class="mb-3">
                        <label for="confirmPassword" class="form-label"  >Confirm your password </label>
                        <input type="password" class="form-control" id="confirmPassword" placeholder="Confirm Password" name="confirmPassword">
                        <span id='message'></span>
                    </div>

                    <button type="button" class="btn btn-info float-right" id="liveToastBtn" onclick="CheckPassword(password);">Register</button>

                </form>

            </div>
        </div>

        <div aria-live="polite" aria-atomic="true" style="position: relative; min-height: 200px;" data-autohide="false">
            <div class="toast" style="position: absolute; top: 10; right: 10;" id="liveToast" data-autohide="false">
                <div class="toast-header">

                    <strong class="mr-auto">User Registered</strong>

                </div>
                <div class="toast-body">
                    Thank you! You are registered :)
                </div>
            </div>
        </div>

        <script>var pw1 = document.getElementById("password");
            var pw2 = document.getElementById("confirmPassword");
            function CheckPassword(inputtxt) {

                var passw = /^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{6,20}$/;
                if (inputtxt.value.match(passw)) {
                    $('#liveToast').toast('show');
                    setInterval(redirectcreateNickname, 3000);
                }
                else {
                    alert('Password does not satisfy the condition')
                    return false;
                }
            }

            $('#password, #confirmPassword').on('keyup', function () {
                if ($('#password').val() == $('#confirmPassword').val()) {
                    $('#message').html('Password Matching 🙂').css('color', 'green');
                } else
                    $('#message').html('Password Not Matching 😮').css('color', 'red');
            });

            function redirectcreateNickname() {
                window.location.href = "/";
            }</script>

</body>